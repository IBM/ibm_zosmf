# Copyright (c) IBM Corporation 2026


DOCUMENTATION = r"""
role: zmf_swmgmt_deployment
short_description: Deploy a portable software instance
description:
    - >
      The B(IBM z/OSMF collection) provides an Ansible role, referred to as
      B(zmf_swmgmt_deployment), to deploy a portable software instance
      to the host system using z/OSMF Software Management.
author:
    - Taylor Digilio (Taylor.Digilio@ibm.com)
options:
    zmf_host:
        description:
            - >
              Hostname of the z/OSMF server, specified in the inventory file
              or as an argument on the playbook command.
        required: True
        type: str
    zmf_port:
        description:
            - >
              Port number of the z/OSMF server.
              If z/OSMF is not using the default port, you need to specify a
              value for this parameter in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
        default: 443
    zmf_user:
        description:
            - User ID for authenticating with the z/OSMF server.
            - >
              Required when not using client certificate authentication
              (C(zmf_client_cert)).
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    zmf_password:
        description:
            - Password to be used for authenticating with z/OSMF server.
            - >
              Required when not using client certificate authentication
              (C(zmf_client_cert)).
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    zmf_client_cert:
        description:
            - >
              Path to a PEM-encoded client certificate file used for authenticating
              with the z/OSMF server.
            - >
              Mutually exclusive with C(zmf_user) and C(zmf_password). When specified,
              basic authentication credentials are not sent.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    zmf_client_key:
        description:
            - >
              Path to the PEM-encoded private key file corresponding to C(zmf_client_cert).
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    zmf_validate_certs:
        description:
            - >
              If set to C(true), the SSL certificate of the z/OSMF server will be validated.
              If set to C(false), the SSL certificate will not be validated.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: bool
        default: false
    deployment_name:
        description:
            - The name for the Deployment object to be created in the z/OSMF data store.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: True
        type: str
    deployment_description:
        description:
            - The description for the Deployment object. This value is optional.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    deployment_submitjobs:
        description:
            - >
              Indicates whether the deployment jobs are to be run after they are generated.
              If the value is true then the jobs will be submitted. If the value is false
              then the jobs will not be submitted and the deployment will be left in progress.
              The jobs can be reviewed, and the deployment may be continued, from the Software
              Management application. The default value is true.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: bool
        default: true
    deployment_source_name:
        description:
            - The name of the portable software instance to deploy.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: True
        type: str
    deployment_source_system:
        description:
            - >
              Nickname of the z/OSMF host system where the source portable software
              instance resides. Use the nickname for the system definition in the z/OSMF
              Systems task.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: True
        type: str
    deployment_target_name:
        description:
            - The name for the software instance to be created by the deployment operation.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: True
        type: str
    deployment_hlq:
        description:
            - High Level Qualifier (HLQ) for all target data sets in the deployment.
            - >
              This is a simplified alternative to specifying C(deployment_configuration).
              When C(deployment_hlq) is provided (without C(deployment_configuration)),
              the role constructs a data set if/then directive with the specified high-level qualifier
              for all target data sets in the deployment configuration.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    deployment_storage_class:
        description:
            - Storage class for all target data sets in the deployment.
            - >
              This is a simplified alternative to specifying C(deployment_configuration).
              When C(deployment_storage_class) is provided (without C(deployment_configuration)),
              the role constructs a data set if/then directive with the specified storage class for
              all target data sets in the deployment configuration.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    deployment_volume:
        description:
            - Volume for all target data sets in the deployment.
            - >
              This is a simplified alternative to specifying C(deployment_configuration).
              When C(deployment_volume) is provided (without C(deployment_configuration)),
              the role constructs a data set if/then directive with the specified volume for
              all target data sets in the deployment configuration.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    deployment_jcldataset:
        description:
            - >
              Name of the partitioned data set on the target system to contain the generated
              deployment JCL. The data set name must comply with z/OS data set naming
              conventions. z/OSMF creates the data set on the target system if it does not
              exist.
            - >
              This is a simplified alternative to specifying C(deployment_systemsettings).
              When C(deployment_jcldataset) is provided (without C(deployment_systemsettings)),
              the role constructs a single-entry C(systemsettings) list for the target system.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    deployment_jobstatement:
        description:
            - >
              The JOB statement for the generated JCL. If specified, it must be a list of
              JCL records, each up to 72 characters long. Columns 1 and 2 of each record
              must be "//" or "/*" and the job name must be 1-8 characters. If not
              provided, the default is "//IZUDnntt JOB (ACCOUNT),'NAME'".
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: list
        elements: str
    deployment_workdsnprefix:
        description:
            - >
              Data set name prefix for work data sets created and deleted during the
              generated JCL run. The prefix can contain up to 26 characters, including
              periods, and must comply with z/OS data set naming conventions. If not
              specified, a default of C(userid.WRKDSN) is used.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    deployment_workvolumes:
        description:
            - >
              Volume serial for work data sets created and deleted during the generated JCL
              run. Must contain 1-6 characters. Valid characters are uppercase alphabetic
              (A-Z), numeric (0-9), and national ($, #, @). Mutually exclusive with
              C(deployment_workstorclas) and C(deployment_workdataclas).
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: list
    deployment_workstorclas:
        description:
            - >
              Storage class for work data sets created and deleted during the generated JCL
              run. Can contain up to eight characters. Valid characters are alphabetic
              (A-Z, a-z), numeric (0-9), and national ($, #, @). Mutually exclusive with
              C(deployment_workvolumes).
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    deployment_workdataclas:
        description:
            - >
              Data class for work data sets created and deleted during the generated JCL
              run. Can contain up to eight characters. Valid characters are alphabetic
              (A-Z, a-z), numeric (0-9), and national ($, #, @). Mutually exclusive with
              C(deployment_workvolumes).
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: str
    deployment_worksmsmanaged:
        description:
            - >
              When C(true), work data sets created and deleted during the generated JCL run
              will be SMS managed and the system determines how and where to allocate them.
              Mutually exclusive with C(deployment_workvolumes), C(deployment_workstorclas),
              and C(deployment_workdataclas).
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: bool
        default: false
    deployment_response_file:
        description:
            - The path to the file that will contain the results from the deployment operation.
            - >
              The directory must already exist otherwise there will be an error writing the
              results to the file. If the file exists in the directory already, it will be
              overwritten by the new response when the playbook is executed. If the file
              doesn't exist in the directory, it will be created.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: True
        type: str
    status_retrieval_retries:
        description:
            - >
              The maximum number of attempts to retrieve the deployment status.
              The role will poll the deployment status until it completes or this
              maximum is reached.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: int
        default: 200
    status_retrieval_delay:
        description:
            - >
              The delay in seconds between each attempt to retrieve the deployment status.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: int
        default: 3
    deployment_configuration:
        description:
            - >
              B(Advanced Deployment Configuration Input) - See sample for advanced input below
              in the Examples section.
            - >
              The properties to modify the deployment configuration which defines the target
              software instance. The deployment configuration is initialized from the source
              software and then modified as described by the following properties.
            - >
              When both C(deployment_configuration) and C(deployment_hlq
              & deployment_storage_class or deployment_volume) are specified, C(deployment_configuration) takes precedence.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: dict
    deployment_systemsettings:
        description:
            - >
              B(Advanced System Settings Input) - See sample for advanced input below
              in the Examples section.
            - >
              Settings used to generate the deployment JCL that may be unique between the
              source and target z/OSMF host systems. Each entry in the list corresponds to
              one system and contains the sub-properties described below.
            - >
              When both C(deployment_systemsettings) and the simplified flat parameters
              (C(deployment_jcldataset), C(deployment_jobstatement), etc.) are specified,
              C(deployment_systemsettings) takes precedence.
            - >
              This variable can be specified in the inventory file or as an argument on
              the playbook command.
        required: False
        type: list
notes:
    - >
      The given example assumes you have an inventory file
      I(inventory.yml) that contains the values for the variables described above,
      such as z/OSMF host server, userid, password, deployment name, source and target
      information, and response file name.
    - >
      Command syntax to call a playbook using an inventory file:
      C(ansible-playbook -i inventory software_management_deployment_CICDtest1.yml)
    - >
      Command syntax to call a playbook using command arguments:
      C(ansible-playbook software_management_deployment_CICDtest1.yml -e zmf_user=zosmf**
      -e zmf_password=zosmf**)
    - >
      When the role is executed, a message shown in following example is
      displayed, C("msg": "Output filename= /tmp/xxx/deployment_response.json").
      This message includes a file path and file name where the deployment information
      is returned.

"""

EXAMPLES = r"""
- name: Sample for simple input
  hosts: sampleHost
  gather_facts: no
  collections:
    - ibm.ibm_zosmf

  tasks:
    - name: Set the deployment configuration and system settings
      ansible.builtin.set_fact:
        deployment_hlq: HLQ
        deployment_storage_class: STANDARD
        deployment_jcldataset: ZOSMF**.P123456.T123456.CNTL
        deployment_workvolumes: ['VOLSER']
    
    - name: Run the deployment role
      ansible.builtin.include_role:
        name: zmf_swmgmt_deployment

- name: Sample for advanced input
  hosts: sampleHost
  gather_facts: no
  collections:
    - ibm.ibm_zosmf

  tasks:
    - name: Set the deployment configuration and system settings
      ansible.builtin.set_fact:
        deployment_configuration:
          datasets:
            - if: "dsname=*.**"
              then:
                - property: "dsname"
                  from: "*.**"
                  to: "*.HLQ.**"
                - property: "storclas"
                  to: "STANDARD"
        deployment_systemsettings:
          deployment_jcldataset: ZOSMF**.P123456.T123456.CNTL
          deployment_workvolumes: ['VOLSER']
    
    - name: Run the deployment role
      ansible.builtin.include_role:
        name: zmf_swmgmt_deployment

"""

# Roles don't return anything.
# But RETURN block must be defined for doc extraction tooling to avoid error.
# The content in RETURN block will not be display in doc-site.
RETURN = r"""
changed:
    description: Indicates if any change is made during the role operation.
    returned: always
    type: bool
"""
